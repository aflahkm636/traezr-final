{
    "name": "minimint-json-server",
    "version": "1.0.0",
    "description": "Mock API using json-server for Minimint",
    "scripts": {
        "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
    },
    "dependencies": {
        "json-server": "^0.17.3"
    }
}
